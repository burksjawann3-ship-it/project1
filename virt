name: Hello VM

on: workflow_dispatch

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - run: echo "Hello from a GitHub virtual machine!"
      - run: uname -a
